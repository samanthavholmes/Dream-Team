<div class= 'container'>
  <h1>Playable Decks</h1>
  <ul>
    <% @decks.each do |deck| %>
      <li>
        <a href="/decks/<%= deck.id %>"><%= deck.deck_name %></a>
      </li>
    <% end %>
  </ul>

<!-- testing flashcard container feature -->

  <div class="flashcard">
    <div class="flashcard-text"><p>Who died in the Triwizard Tournament?</p>
  </div>


</div>
